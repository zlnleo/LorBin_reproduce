# 00 · 顺序指南：从零手写 LorBin 实验环境

> ## 这份指南怎么用
> 你**自己手写** `Dockerfile` / `docker-compose.yaml` / `.dockerignore`，自己构建镜像，自己在容器里跑通 LorBin。
>
> 本指南只给你三样东西：**① 顺序（先做什么、后做什么、为什么）② 每一步的"要求清单" ③ 每一步的验证方法与期望结果**。
> **它不给现成文件内容** —— 我验证过的那一份**就在 `fromscratch\` 里的规范位置**（`docker\Dockerfile`、`docker\docker-compose.yaml`、`.dockerignore`），**写不出来或想核对了再看**。
>
> 唯一可以直接用的是 `fromscratch\tools\env_probe.sh`：它不是"答案"，是**验证工具**（无时间戳的环境快照）。
>
> ### ⚠️ 动手前先做一件事：把标准答案另存一份
> `fromscratch\` 现在是**规范答案状态**（三个文件都在正确位置，镜像 `lorbin-tutorial:1.0` 已构建好）。
> 你要手写这三个文件，就会**覆盖掉标准答案**。所以先备份：
> ```cmd
> cd /d D:\project\article\LorBin\fromscratch
> mkdir _标准答案备份
> copy .dockerignore            _标准答案备份\
> copy docker\Dockerfile        _标准答案备份\
> copy docker\docker-compose.yaml _标准答案备份\
> ```
> 然后你手写自己的版本，写完用 `fc` 逐行对比（见每步的"对照答案"）：
> ```cmd
> fc .dockerignore            _标准答案备份\.dockerignore
> fc docker\Dockerfile        _标准答案备份\Dockerfile
> fc docker\docker-compose.yaml _标准答案备份\docker-compose.yaml
> ```
> 对比的原则：**不要求逐字一样，但要求你的每一条都能说出理由**；
> 不一样的地方，问自己"我这样写会导致什么后果"（例如漏掉 `**/` 前缀 → 上下文变大）。
>
> ## 三条铁律（贯穿全部步骤）
> 1. **环境进镜像，数据进挂载卷** —— 镜像里绝不放数据集
> 2. **每写完一个文件，立刻用一条命令验证它** —— 别攒到最后一起 debug
> 3. **看到"检查点"必须真的看到，才算这一步过了** —— 不许"应该没问题"就往下走

---

## 0. 你要造出什么（最终验收）

| # | 产物 | 验收标准 |
|---|---|---|
| 1 | 手写的三个文件 | `fromscratch\.dockerignore`、`fromscratch\docker\Dockerfile`、`fromscratch\docker\docker-compose.yaml`，且**每个决定你都能解释** |
| 2 | 自己构建的镜像 | 能跑 LorBin，体积 **~1.8 GB**（不是 6.4 GB），`docker history` 里 `COPY` 那层只有十几 MB |
| 3 | 跑通的结果 | `label.csv` 的 md5 = `a12528741f32dda52709f99cdea6af6b`，`output_bins` 里 **93** 个 `.fa` |
| 4 | 可复现证据 | 删掉镜像能重建；清空输出能重跑一致；数据确认是挂载的 |

**反面参照（本工作区里就有）**：镜像 `lorbin-repro:1.0` = **6.4 GB**，因为它的 `.dockerignore` 没写对，把 2.09 GB 的数据集 COPY 进了镜像。
你要造的是 `lorbin-tutorial:1.0` 那样的：**1.76 GB**。

---

## 1. 总顺序图

### 1.1 十二步一览

| 步 | 动作 | 产物 / 动作 | 预计 | 依赖 |
|---|---|---|---|---|
| **S0** | 前置检查 | 确认引擎、目录、磁盘 | 2 min | — |
| **S1** | 读 README | 填完"环境信息卡" | 15 min | S0 |
| **S2** | 决定策略 | 选定基础镜像，写下理由 | 10 min | S1 |
| **S3** | 建目录骨架 | `docker\` `data\` `runs\` | 2 min | S0 |
| **S4** | 写 `.dockerignore` | `fromscratch\.dockerignore` | 15 min | S3 |
| **S5** | 写 `Dockerfile` | `docker\Dockerfile` | 40 min | S4 |
| **S6** | 写 `docker-compose.yaml` | `docker\docker-compose.yaml` | 20 min | S5 |
| **S7** | **下载数据集** | 两个文件 + md5 核对 | **14 min** | — |
| **S8** | 构建镜像 | `lorbin-tutorial:1.0` | 4 min | S4–S6 |
| **S9** | 运行期自检 | `logs\env_build1.txt` | 1 min | S8 |
| **S10** | 跑主流程 | `runs\out1\label.csv` | **12 min** | S7+S9 |
| **S11** | 与 baseline 对比 | 对照表 | 5 min | S10 |
| **S12** | 可复现性四问 | 4 条证据 | 20 min | S11 |

**合计约 2.5 小时**（其中 26 分钟是纯等待）。

### 1.2 依赖关系（谁能和谁并行）

```text
S0 前置检查
 └─► S1 读 README ──► S2 决定策略 ──► S3 建目录
                                        │
                        ┌───────────────┼───────────────┐
                     S4 .dockerignore  S5 Dockerfile   S6 compose      ← 三者有先后（见 §1.3）
                        └───────────────┼───────────────┘
                                        ├──────────────┐
                                  S7 下载数据        S8 构建镜像
                                  (14 min，可并行)      │
                                        │            S9 运行期自检
                                        └──────┬─────┘
                                            S10 跑主流程（12 min）
                                               │
                                            S11 对比 baseline
                                               │
                                            S12 可复现性四问
```

⭐ **最值得利用的一点**：**S7 下载数据不依赖任何文件**。
所以正确做法是：**先把 `curl` 挂上（或后台跑），在等待的 14 分钟里写 S4–S6 三个文件**。
这样 14 分钟不浪费；等文件写完，数据也就位了，可以直接进 S8。

### 1.3 三个文件的先后为什么是这个顺序

| 顺序 | 为什么不能颠倒 |
|---|---|
| **先 `.dockerignore`，再 `Dockerfile`** | `Dockerfile` 里 `COPY` 的范围**由 `.dockerignore` 决定**。先写前者，你才能在 S8 第一次构建时立刻看到"上下文只有十几 MB"这个证据；反过来先写 Dockerfile，你会先构建出一个几 GB 的镜像，再回头排查 |
| **先 `Dockerfile`，再 `compose`** | compose 里要引用镜像名、要挂载与工作目录 —— 这些是你写 Dockerfile 时才定下来的（`WORKDIR`、容器内路径约定）。先有前者，compose 才有内容可写 |
| **`COPY 源码` 放 Dockerfile 最后一层** | 层的缓存特性：源码改动最频繁。放最后 → 改一行代码重建只要几秒，而不是重装一遍依赖 |

---

## 2. 概念速查（写文件之前先把这 5 条看懂）

### 2.1 层（layer）与缓存

每个 `RUN` / `COPY` 生成一层。**只要某一层及其之前的所有输入没变，重建时就复用缓存**。
所以层的顺序应该按"**变化频率从低到高**"排：系统包 → 语言依赖 → 重依赖(torch) → 自检 → **源码**。

### 2.2 构建上下文（context）与 `.dockerignore`

`docker build` 会把**整个上下文目录**打包发给 docker daemon。
上下文由命令最后那个参数决定（本项目是 `fromscratch\`）。

**上下文越小越好**：本项目的正确值是**十几 MB**（主要是源码里 16 MB 的 `marker.hmm`）。
如果数据被卷进上下文，每次构建都要先传 2 GB —— 慢、占磁盘，而且毫无必要。

#### ⚠️ 一条必须记住的规则：`*` **不跨 `/`**

`.dockerignore` 的模式匹配**和 Go 的 `filepath.Match` 一致**，因此：

| 你写的 | 实际匹配 | 匹配不到 |
|---|---|---|
| `*.tar.gz` | **只匹配根目录**下的 `a.tar.gz` | ❌ `LorBin/testdata.tar.gz` |
| `**/*.tar.gz` | 任意层级 | — |

**真实教训（本工作区可复现）**：`lorbin-repro:1.0` 那个 6.4 GB 的镜像，
它的 `.dockerignore` 里写了 `LorBin/testdata/`（只匹配目录）和 `*.tar.gz`（只匹配根目录），
结果 `LorBin/testdata.tar.gz`（**2.09 GB**）两条都没匹配上，被 COPY 进镜像 ——
`docker history` 显示那一层 **2.13 GB**。写成 `**/*.tar.gz` 之后，同一层只有 **16.6 MB**。

> **两条自查习惯**（比记语法更重要）：
> 1. 构建开头那行 `transferring context: XX MB` —— **超过几十 MB 就要查**
> 2. 构建完 `docker history <镜像>` —— **`COPY` 层如果是 GB 级，就是数据混进去了**

**位置也很关键**：`.dockerignore` 必须放在**上下文根目录**（本项目是 `fromscratch\`，不是 `docker\` 里）。
放错位置 = 没写。

### 2.3 挂载（volume）vs 拷贝（COPY）—— 为什么数据必须挂载

| 方式 | 写法 | 用途 | 本项目 |
|---|---|---|---|
| **bind mount（绑定挂载）** | `-v 宿主目录:容器目录:ro` | 数据在宿主机某个目录 | ⭐ **输入数据与输出结果都用它** |
| **named volume** | `-v 卷名:容器目录` | 由 Docker 管理、多容器共享 | 不需要（我们要能直接看到宿主机文件） |
| ~~`COPY` 进镜像~~ | `COPY data /data` | 只适合几 KB 的默认配置 | ❌ **禁止** |

**为什么数据不能进镜像（四条，都有数字）**：

1. **镜像会变成超大文件** —— 数据压缩包 2.09 GB，解压后 2.2 GB。本工作区那个 6.4 GB 的镜像就是活教材
2. **镜像失去分享价值** —— 别人用你的镜像是想借**环境**，不是想借你的**数据**
3. **数据一换就得重建镜像** —— 今天跑 demo、明天跑真实样本，难道每次重新 build？
4. **构建上下文会爆炸** —— 数据在上下文里，每次构建都要先传 2 GB

👉 正确分工：**镜像 = 环境（几百 MB~2 GB，可分享可重建）；卷 = 数据（几 GB，留在宿主机）**。

输出的结果同理：`-o` 必须指向**挂载目录**，否则容器 `--rm` 一删，跑 12 分钟的结果就没了。

### 2.4 版本 pin 的取舍

仓库 `lorbin_env.yaml` 里写的版本是"**作者写论文时的版本**"，不等于"现在装得上"。

- 能对齐的就对齐（numpy / scipy / pandas / scikit-learn）
- 装不上或没必要的就换有预编译 wheel 的等价版本，**并把原因写进注释**
  - 例：官方 `biopython=1.78` 比 Python 3.10 还早，**没有 cp310 的 wheel**，pip 会退化去源码编译
  - 例：官方 `torch=1.11.0` 很老；用 CPU 版还能省 2 GB

**关键不是选哪个版本，而是"偏离的原因写进注释"** —— 半年后你才知道当时为什么这么定。

### 2.5 三个文件各管什么

| 文件 | 管什么 | 类比 |
|---|---|---|
| `.dockerignore` | **哪些文件不进构建上下文** | 打包清单的排除项 |
| `Dockerfile` | **环境怎么造出来**（系统包、语言依赖、源码） | 环境的源代码 |
| `docker-compose.yaml` | **造好之后怎么跑**（挂载、工作目录、命令） | 启动器配置 |

---

## 3. 逐步：S0 – S12

> 每步格式：**目标 → 你要做什么 → 要求清单 → 验证 → 卡住提示 → 对照答案**

### S0 · 前置检查

**目标**：确认 Docker 引擎可用、磁盘够、工作区就位。

```cmd
docker --version
docker info --format "{{.ServerVersion}} {{.OSType}} {{.NCPU}} {{.MemTotal}}"
docker images --format "{{.Repository}}:{{.Tag}} {{.Size}}" | findstr lorbin
cd /d D:\project\article\LorBin\fromscratch
dir
```

**要求**
- [ ] `docker info` 能打印，且 **`OSType` 必须是 `linux`**
- [ ] 磁盘剩余 ≥ 20 GB（镜像 ~2 GB + 数据 ~4.5 GB + 构建缓存）
- [ ] `fromscratch\` 下有：`LorBin\`（官方源码）、`baseline\golden\`（参照结果）、`tools\env_probe.sh`、`docker\`（空目录，等你写）

**检查点**
- `docker images` 能看到 `lorbin-tutorial:1.0`（1.76 GB，手写版参照）和 `lorbin-repro:1.0`（6.4 GB，反面参照）
- ❌ 报 `failed to connect to the docker API at npipe:...` → 引擎没启动，等 1–3 分钟

> **想彻底从零？** `docker rmi lorbin-tutorial:1.0` 把它删掉（`fromscratch\docker\Dockerfile` 随时能重建它）。

---

### S1 · 读 README，填"环境信息卡"

**目标**：从 README 里**主动提取**出写 Dockerfile 所需要的全部信息。

**你要做什么**：打开 `fromscratch\LorBin\README.md`，把下面这张卡填完（**先别看答案，自己找**）。

```
┌─ 环境信息卡 ─────────────────────────────────────────────┐
│ ① 支持的操作系统        :                                │
│ ② 语言运行时 + 版本     :                                │
│ ③ 系统二进制依赖（几个）:                                │
│ ④ Python 包依赖（几个） :                                │
│ ⑤ 官方给的安装方式      :                                │
│ ⑥ 数据从哪来            :                                │
│ ⑦ 输入文件格式要求      :                                │
│ ⑧ 主命令                :                                │
└──────────────────────────────────────────────────────────┘
```

**要求**
- [ ] 8 项都填上，并**写下它在 README 的哪一段/哪一行**
- [ ] ③ 的"系统二进制依赖"要能列出**全部 5 个**工具名
- [ ] 额外回答两个判断题：
  - [ ] **这些工具在 Windows 上有可用的安装包吗？** 这决定了要不要 Docker
  - [ ] **README 的依赖清单和仓库里的 `lorbin_env.yaml` 完全一致吗？** 自己 diff 一下
    ```cmd
    git -C fromscratch\LorBin ls-files | findstr /i "env yaml"
    type fromscratch\LorBin\lorbin_env.yaml
    ```
    找出 README 里有、而 `lorbin_env.yaml` 里没有的那个工具

**检查点**：你能不看书回答"这个软件要什么系统、什么依赖、什么输入、什么命令"。

**卡住提示**
- 依赖分两类，别混：**Python 包**（pip 装）和 **系统二进制**（apt 装）
- README 里 `## System Requirements` / `### Required dependencies` / `## Preprocessing` / `## How to run LorBin` 这四段就是答案所在

**对照答案**：本指南 §4.1 有一张填好的卡，**填完再看**。

---

### S2 · 决定环境策略，选定基础镜像

**目标**：写下"要不要自己写 Dockerfile"和"用哪个基础镜像"的**理由**（答辩会被问）。

**你要做什么**

```cmd
:: 关键一步：这个仓库到底有没有现成的 Docker 支持？
git -C fromscratch\LorBin ls-files | findstr /i "docker compose"
:: 有没有别的环境定义？
git -C fromscratch\LorBin ls-files | findstr /i "env yaml requirements"
```

**要求**
- [ ] 走一遍决策树，并把结论写在你的笔记里：

```text
仓库有 Dockerfile/compose 吗？
├─ 有 ──► 直接用。但仍要检查两件事：
│         ① 它有没有把数据 COPY 进镜像？（有 → 改成挂载）
│         ② 它的 README 里"怎么用这个 Docker"怎么说
└─ 没有 ─► 有 conda/requirements 吗？
          ├─ 有 ─► 本机 OS 与 README 要求一致吗？
          │        ├─ 一致（Linux/macOS）→ 直接用 conda，最省事
          │        └─ 不一致（Windows）   → ⭐ 自己写 Dockerfile ← 本项目在这
          └─ 没有 ─► 自己从 README/代码列依赖，再写 Dockerfile
```

- [ ] 选定基础镜像，并说清"**为什么是它**"。判断依据三个：
  - [ ] 语言运行时版本对不对（README 说了要哪个 Python）
  - [ ] 系统包源里有没有你要的那 5 个工具
  - [ ] 体积

**检查点**：`findstr /i "docker compose"` **没有任何输出** → 仓库不提供 Docker。
**这一步的结论直接决定你接下来是做"用别人的 Dockerfile"还是"自己写"—— 这是整套流程里最重要的判断动作。**

**卡住提示**：什么发行版自带的 Python 版本正好是 README 要求的那个？用什么命令可以查某个包在不在源里？
（提示：`docker run --rm <基础镜像> apt-cache policy <包名>`）

**对照答案**：`fromscratch\docker\Dockerfile` 第 1 行往下看注释。

---

### S3 · 建目录骨架

**目标**：把"上下文根目录"和"运行期目录"分清楚。

**要求**
- [ ] 目录结构长这样（本工作区已经建好，你核对一遍即可）：

```
fromscratch\                  ← ⭐ 构建上下文根目录
├─ .dockerignore              ← S4 写这里（位置！）
├─ docker\
│  └─ Dockerfile              ← S5 写这里
│  └─ docker-compose.yaml     ← S6 写这里
├─ LorBin\                    ← 官方源码（已是干净 clone）
├─ data\                      ← 输入数据（挂载到容器 /data:ro）
├─ runs\                      ← 运行结果（挂载到容器 /work/runs）
├─ logs\                      ← 日志/环境快照
├─ baseline\golden\           ← 参照结果（挂载到容器 /work/baseline:ro）
└─ tools\env_probe.sh         ← 验证工具
```

- [ ] 能回答：**为什么 `.dockerignore` 不能放进 `docker\` 里？**

**检查点**：`data\` 现在是空的（S7 才填）；`docker\` 里除了一个 `README.txt` 提示外是空的。

---

### S4 · 写 `.dockerignore`（**先写这个**）

**这一步的产物**：`fromscratch\.dockerignore`

**要求（逐条满足即可，不要求和别人写得一样）**
- [ ] 位置在**上下文根目录** `fromscratch\` 下
- [ ] 排除**数据**：数据目录、压缩包、`*.bam` / `*.bai` / `*.fna` / `*.fa` / `*.fastq`
- [ ] 排除**结果与日志**：`runs\`、`logs\`、`baseline\`
- [ ] 排除**源码目录里的噪音**：`.git\`、`__pycache__\`、`*.pyc`、打包产物（`build\`、`dist\`、`*.egg-info\`）
- [ ] ⚠️ **每个"按扩展名排除"的规则都要考虑"会不会有子目录"** —— 用 `**/` 前缀（见 §2.2）
- [ ] 用注释写清每条规则为什么在（这份文件经常需要维护）

**验证（这一步就能验，不用等构建）**

```cmd
:: ① 文件在不在正确位置
dir fromscratch\.dockerignore

:: ② 干跑一次构建，只看上下文那一行（Ctrl+C 中断即可）
cd /d D:\project\article\LorBin\fromscratch
docker build -f docker\Dockerfile -t test-ctx .
::   ⭐ 看这行：#5 transferring context: XX MB
```

**检查点**
| 看到的值 | 判定 |
|---|---|
| **十几 MB** | ✅ 通过（这点体积主要是源码里 16 MB 的 `marker.hmm`） |
| 几百 MB ~ 几 GB | ❌ 有东西没被排除 → 回去检查通配符是否跨目录 |

**卡住提示**：如果上下文很大，先问自己"**2 GB 的数据集文件放在哪个路径？我的规则能匹配到它吗？**"
用 `**/` 试试；也可以临时在 `data\` 放一个 1 MB 的假文件，看它是否影响上下文大小（验证手段比猜快）。

**对照答案**：`fromscratch\.dockerignore`（含那条 2.13 GB 教训的完整注释）

---

### S5 · 写 `docker\Dockerfile`

**这一步的产物**：`fromscratch\docker\Dockerfile`

**要求（按这个结构写，每层都要能说清"为什么单独一层"）**
- [ ] **基础镜像**：S2 选定的那个，并用注释写下理由
- [ ] **环境变量**：至少要有"apt 不弹交互提示"这一个（否则构建会卡住）；建议再加 Python 日志实时输出
- [ ] **第 1 层 · 系统依赖**：一条 `apt-get install` 装齐 5 个工具 + Python；用 `--no-install-recommends` 省体积；
      **在同一个 `RUN` 里**清掉 apt 索引缓存（写到下一层删是没用的，体积已经固化了）
- [ ] **第 2 层 · Python 依赖**：装全 LorBin 需要的包；对官方 pin 里"装不上"的版本做取舍，**把原因写进注释**
- [ ] **第 3 层 · PyTorch**：CPU 版（为什么？写进注释）；需要指向 PyTorch 官方的 CPU wheel 源
- [ ] **第 4 层 · 构建期自检**：让**每个工具都打印版本**，Python 依赖也 import 一遍；
      意图是 **fail fast** —— 缺任何一个，构建立刻失败，而不是等到跑了两小时才发现
- [ ] **第 5 层 · 源码**：`COPY` 源码进来 + `pip install` 本地目录 + 跑一次 `LorBin --help` 确认 CLI 装上了；
      ⚠️ **绝对不要 COPY 数据**
- [ ] **运行期约定**：`WORKDIR`（容器里的工作目录，要和 S6 的挂载点对齐）+ 默认给个交互 shell
- [ ] **层顺序**：源码 `COPY` 在最后

**验证（构建并逐项对照）**

```cmd
cd /d D:\project\article\LorBin\fromscratch
docker build -f docker\Dockerfile -t lorbin-tutorial:1.0 ^
  --build-arg http_proxy=http://host.docker.internal:7897 ^
  --build-arg https_proxy=http://host.docker.internal:7897 .
```

**检查点（三处，都要看到）**
1. **上下文大小**：`transferring context:` → 十几 MB
2. **自检输出**（三段）：

```text
--- 外部生信工具 ---
Prodigal V2.6.3
# HMMER 3.3.2 (Nov 2020); http://hmmer.org/
bedtools v2.30.0
samtools 1.13
2.24-r1122
--- Python 依赖 ---
numpy 1.23.3 | ... | torch 2.5.1+cpu | Bio 1.83
--- LorBin CLI OK ---
```

3. **镜像大小**：`docker images lorbin-tutorial` → **~1.8 GB**
   再补一刀：`docker history lorbin-tutorial:1.0` → **`COPY` 那层应该只有十几 MB**

**卡住提示（这三个坑是真实踩过的）**
- ⚠️ **prodigal 的版本串里没有 "version" 这个词**：`prodigal -v` 输出 `Prodigal V2.6.3: February, 2016`。
  所以 `grep -i version` 取不到值（会输出空行）。用 `grep -io 'Prodigal V[0-9.]*'`。
- ⚠️ 如果 `pip install` 某个包在编译（而不是下载 wheel），多半是**那个版本没有对应 Python 的预编译包** → 换版本。
- ⚠️ 网络受限时构建期要传代理（`host.docker.internal` 指宿主机）；**运行阶段不需要出网**。

**对照答案**：`fromscratch\docker\Dockerfile`（逐层注释）、`fromscratch\logs\build.log`（完整构建日志，可对照你的输出）

---

### S6 · 写 `docker\docker-compose.yaml`

**这一步的产物**：`fromscratch\docker\docker-compose.yaml`

**要求**
- [ ] 一个 service，名字自定（下文以 `lorbin` 代称）
- [ ] `build.context` 指向**上下文根目录**（注意：compose 里的相对路径是**相对 compose 文件所在目录**解析的）
- [ ] `build.dockerfile` 指向 Dockerfile 相对 context 的路径
- [ ] `build.args` 支持代理（构建期需要联网）
- [ ] `image:` 给镜像起个名字
- [ ] `working_dir:` 和 Dockerfile 的 `WORKDIR` 对齐
- [ ] `volumes:` 四条挂载：
  - [ ] 输入数据 → **只读**（`:ro`，容器不可能写坏原始数据）
  - [ ] 输出目录 → 可写
  - [ ] 日志 → 可写
  - [ ] 基线结果 → 只读
  - [ ] 验证工具 `tools\` → 只读
- [ ] `stdin_open: true` + `tty: true`（否则 `run ... bash` 进不去交互 shell）
- [ ] `command:` 默认给个 shell，便于覆盖
- [ ] 写清"数据为什么用挂载而不是 COPY"的理由（注释）

**验证**

```cmd
cd /d D:\project\article\LorBin\fromscratch

:: ① 语法与解析（最重要的验证：把 YAML 展开成最终配置给你看）
docker compose -f docker\docker-compose.yaml config

:: ② 用 compose 跑一条命令
docker compose -f docker\docker-compose.yaml run --rm lorbin LorBin --help
```

**检查点**
- `config` 输出里能看到你写的所有 `volumes`，且宿主机路径被解析成了**绝对路径**、指向正确的位置
- `LorBin --help` 能打印出子命令列表（`bin` / `generate_data` / `train` / `cluster` / `concat`）
- 若报挂载路径错误 → 检查相对路径基准（相对 compose 文件，不是相对你当前所在的目录）

**卡住提示**：`docker compose config` 是排 YAML 问题的最快工具；它会把变量展开、路径解析结果全部打印出来。

**对照答案**：`fromscratch\docker\docker-compose.yaml`

---

### S7 · 下载数据集（**尽早开始，可与 S4–S6 并行**）

**目标**：把数据放到**挂载目录** `fromscratch\data\`，并核对 md5。

| 项 | 值 |
|---|---|
| 来源 | Zenodo 记录 **13883404**（README 里给的链接） |
| 直链 | `https://zenodo.org/api/records/13883404/files/testdata.tar.gz/content` |
| 大小 | **2,091,328,540** 字节 |
| md5（压缩包） | `e6378d903c39d9a383d815ff85797f6d` |
| ⚠️ 注意 | ① **不支持断点续传**（带 `Range` 头会被拒）→ 一次下完 ② 本机 DNS 把 `zenodo.org` 屏蔽成 0.0.0.0 → **必须走代理** |

```cmd
cd /d D:\project\article\LorBin\fromscratch\data

curl -L --proxy http://127.0.0.1:7897 -o testdata.tar.gz ^
     "https://zenodo.org/api/records/13883404/files/testdata.tar.gz/content"

certutil -hashfile testdata.tar.gz MD5          :: 期望 e6378d903c39d9a383d815ff85797f6d
tar -xzf testdata.tar.gz                        :: 两个文件解压后就在 data\ 下（不会再套一层目录）
del testdata.tar.gz                             :: 可选，省 2 GB

certutil -hashfile CRR451057.hifiasm.fna MD5    :: 期望 5725ee31ae70727cabf90fb8fa6c58b4   167,712,357 B
certutil -hashfile CRR451057.sorted.bam MD5     :: 期望 76de2a896edea7b3d79145722e8c14e5 2,050,810,166 B
dir
```

**检查点**
- [ ] `data\` 下**直接**有那两个文件（没有多余的子目录）
- [ ] 两个 md5 都对上（**先验压缩包再解压**，包错了就别浪费时间解压）

**卡住提示 / 省时替代**：工作区 `reproduce\LorBin\testdata\` 里已有同一份数据（md5 已核对过），
可以直接 `copy /Y` 到 `fromscratch\data\` —— 这不算"复用别人的成果"，它只是官方数据集本身。
但你若想让"数据完整性核对"这一环也走完整流程，就重新下一次。

---

### S8 · 构建镜像

```cmd
cd /d D:\project\article\LorBin\fromscratch

:: 方式 A：docker 原生（-f 指定 Dockerfile，最后那个 . 是上下文）
docker build -f docker\Dockerfile -t lorbin-tutorial:1.0 .

:: 方式 B：compose（参数都写在 yaml 里）
docker compose -f docker\docker-compose.yaml build

:: 网络受限时（构建期要联网装包）
docker build -f docker\Dockerfile -t lorbin-tutorial:1.0 ^
  --build-arg http_proxy=http://host.docker.internal:7897 ^
  --build-arg https_proxy=http://host.docker.internal:7897 .
```

**检查点**
- [ ] 所有构建步都 `DONE`，没有 `ERROR`
- [ ] 三段自检都打印出正确版本
- [ ] `docker images lorbin-tutorial` → **~1.8 GB**
- [ ] `docker history lorbin-tutorial:1.0` → `COPY` 层只有十几 MB
- [ ] 首次约 3–4 分钟；**再构建一次应该秒级完成**（缓存生效 —— 这也是验证你层顺序写对了的办法）

**卡住提示**：构建失败按这个顺序定位 → ① 网络/代理 ② 版本不存在（wheel 下架/拼错）③ 上下文太大

---

### S9 · 运行期环境自检（并**存档**）

**目标**：证明"在真实挂载与权限下也能用"，并把结果存下来以便日后对比。

```cmd
cd /d D:\project\article\LorBin\fromscratch
docker run --rm -v D:\project\article\LorBin\fromscratch:/work lorbin-tutorial:1.0 bash /work/tools/env_probe.sh > logs\env_build1.txt
type logs\env_build1.txt
```

**检查点**
- [ ] 5 个工具都有版本号
- [ ] Python 依赖 7 项齐全（含 `cuda_available False`）
- [ ] `LorBin CLI: /usr/local/bin/LorBin`
- [ ] `marker.hmm: OK`、`model/huigui_weights.pt: OK`
- [ ] ⚠️ `model/*_markers35.pt` 显示 **MISSING 是正常的** —— 官方仓库里就没有这几个文件
      （所以别用 `--evaluation markers35`，会 `FileNotFoundError`）

---

### S10 · 跑主流程

```cmd
:: compose 版（前台，直接看日志）
docker compose -f docker\docker-compose.yaml run --rm lorbin ^
  LorBin bin --fa /data/CRR451057.hifiasm.fna -b /data/CRR451057.sorted.bam ^
             -o /work/runs/out1 --num_process 12

:: 原生版（后台跑，长任务更稳）
docker run -d --name lorbin-run1 ^
  -v D:\project\article\LorBin\fromscratch\data:/data:ro ^
  -v D:\project\article\LorBin\fromscratch\runs:/work/runs ^
  -w /work lorbin-tutorial:1.0 ^
  bash -c "LorBin bin --fa /data/CRR451057.hifiasm.fna -b /data/CRR451057.sorted.bam -o /work/runs/out1 --num_process 12 > /work/runs/run1.log 2>&1"
```

**⚠️ 最容易犯的错**：`-o` 必须指向**挂载目录**（`/work/runs/...`）。
指到别处 → 跑 12 分钟，容器一删结果就没了。

**检查点（用"出现了哪个文件"判断进度，全程约 11–12 分钟）**

| 出现的文件 | 阶段 | 累计 |
|---|---|---|
| `LorBin.log` | 启动 | 0 min |
| `length.csv` | 覆盖度算完 | ~2 min |
| `data.csv` | 特征矩阵（13,619 × 139） | ~2.5 min |
| `markers.hmmout` | marker 检测完 | ~5 min |
| `model.pt`、`embedding.csv` | VAE 训练完（300 轮） | ~9.5 min |
| `label.csv`、`output_bins\` | ✅ 完成 | ~11 min |

```cmd
docker ps -a --filter name=lorbin-run1 --format "{{.Status}}"     :: 期望 Exited (0)
dir /b runs\out1
```

---

### S11 · 与 baseline 对比

```cmd
cd /d D:\project\article\LorBin\fromscratch

certutil -hashfile runs\out1\label.csv     MD5    :: 期望 a12528741f32dda52709f99cdea6af6b
certutil -hashfile baseline\golden\label.csv MD5  :: 应与之完全相同
certutil -hashfile runs\out1\embedding.csv MD5    :: 期望 7cbf2784b2b0ac09e794a6583a46146c
certutil -hashfile runs\out1\model.pt      MD5    :: 期望 8dbfa00348f38df215be5ec0901db1f3
dir /b runs\out1\output_bins\*.fa | find /c ".fa" :: 期望 93
```

**检查点**
- [ ] `label.csv` 与 baseline **逐字节相同**
- [ ] `output_bins` 有 **93** 个 `.fa`
- [ ] 能解释：为什么 `data.csv` / `*_data_cov.csv` 的 md5 **不同也不算错**？

**⚠️ 必须理解的一处差异**
覆盖度产物的**列名 = 命令行传入的 BAM 路径字符串 + `_cov`**。
baseline 跑的时候 BAM 在 `/work/reproduce/LorBin/testdata/...`，你这次在 `/data/...` → **列名不同 → md5 不同**。
但**数值完全一样，而且这个列名不会流到下游**（训练只用 `data.values`），
所以 `label.csv` / `embedding.csv` / `model.pt` 的 md5 **不受影响**。
👉 这正好是一课：**md5 一致 ≠ 结果一致**，要分清"哪些字节是结果、哪些只是元信息"。

---

### S12 · 可复现性四问（**别跳过，这才是"复现"**）

```cmd
cd /d D:\project\article\LorBin\fromscratch

:: ① 能重建吗？删掉镜像，只留 Dockerfile，重新建一次
docker rmi lorbin-tutorial:1.0
docker build -f docker\Dockerfile -t lorbin-tutorial:1.0 .
docker run --rm -v D:\project\article\LorBin\fromscratch:/work lorbin-tutorial:1.0 bash /work/tools/env_probe.sh > logs\env_build2.txt
fc logs\env_build1.txt logs\env_build2.txt        :: ✅ 无差异 = 环境可重建

:: ② 能重跑一致吗？清空输出目录再跑一次（约 12 分钟），比 label.csv
rmdir /s /q runs\out2
docker run --rm -v D:\project\article\LorBin\fromscratch\data:/data:ro -v D:\project\article\LorBin\fromscratch\runs:/work/runs ^
  -w /work lorbin-tutorial:1.0 LorBin bin --fa /data/CRR451057.hifiasm.fna ^
  -b /data/CRR451057.sorted.bam -o /work/runs/out2 --num_process 12
certutil -hashfile runs\out2\label.csv MD5        :: ✅ 应与 out1 相同

:: ③ 数据是挂载的吗？
docker image inspect lorbin-tutorial:1.0 --format "{{.Size}}"
docker history lorbin-tutorial:1.0                :: ✅ COPY 层应只有十几 MB

:: ④ 别人能跑吗？把 fromscratch\ 去掉 data\ runs\ baseline\ 打包给别人，他照本指南能跑出同样结果
```

**检查点：四个问题都能回答"是"**，并把证据（两张环境快照的 `fc` 结果、两次 `label.csv` 的 md5）记到验收表里。

---

## 4. 参考：几处容易搞错的地方

### 4.1 环境信息卡（**填完再看**）

| # | 项 | 答案 | 在 README 的位置 |
|---|---|---|---|
| ① | 支持的操作系统 | **Linux**（"supported and tested in Linux systems"） | `## System Requirements` |
| ② | 语言运行时 + 版本 | **Python 3.10** | `## System Requirements` 首句 |
| ③ | 系统二进制依赖 | **prodigal、hmmer(hmmsearch)、bedtools、samtools、minimap2**（5 个） | `### Required dependencies` → Sequence processing tool |
| ④ | Python 包依赖 | biopython、pytorch、numpy、pandas、scikit-learn、scipy、joblib、setuptools | 同段 python modules |
| ⑤ | 官方安装方式 | **conda**（`conda env create -f lorbin_env.yaml`）+ `pip install dist/lorbin-0.1.0.tar.gz` | `## Installation` |
| ⑥ | 数据来源 | **Zenodo 记录 13883404**（`testdata.tar.gz`） | `## A test dataset to demo LorBin` |
| ⑦ | 输入格式 | contigs **FASTA** + **已排序**的 BAM（`SO:coordinate`） | `## Preprocessing` |
| ⑧ | 主命令 | `LorBin bin --fa <contigs.fna> -b <sorted.bam> -o <outdir>` | `## How to run LorBin` |

**两个判断题的答案**
- **Windows 上行不行？** ❌ 不行。这 5 个工具都是 Unix 生信工具，Windows 上没有可用的 conda 构建 → **这就是必须用 Docker 的根本原因**（不是偏好问题，是依赖可用性问题）
- **README 和 `lorbin_env.yaml` 一致吗？** ❌ 不一致：README 列了 **minimap2**，`lorbin_env.yaml` 里**没有** → 以 README 为准，把它补上
  - 附带一个有价值的判断：**LorBin 本体其实从不调用 minimap2**（代码里只调 **bedtools / prodigal / hmmsearch**）。minimap2 只用于 README 的上游预处理（把原始 reads 比对成 BAM）。所以只跑 LorBin 不用它，想走完整预处理才需要

### 4.2 常见报错 → 原因 → 处理

| 现象 | 原因 | 处理 |
|---|---|---|
| 构建卡在 `Sending build context to Docker daemon` 很久 | ⭐ 上下文太大，`.dockerignore` 没生效或通配符没跨目录 | 见 §2.2；确认文件在**上下文根目录**、用 `**/` 前缀 |
| `apt-get update` 失败 / 域名解析不了 | 构建期要联网，本机 DNS 受限 | 传 `--build-arg http_proxy=... https_proxy=...` |
| `zenodo.org` 下不动 | 本机 DNS 把它屏蔽 | `curl --proxy http://127.0.0.1:7897` |
| `pip install` 在编译某个包 | 该版本没有对应 Python 的 wheel | 换有 wheel 的版本，并把原因写进注释 |
| `apt-get install` 找不到某个生信工具 | 基础镜像是最小化版（没启用 universe 源） | 换官方 `ubuntu:22.04`，或启用 universe |
| 自检里 prodigal 版本是空行 | `grep -i version` 匹配不上（输出里没有 "version"） | `grep -io 'Prodigal V[0-9.]*'` |
| 镜像莫名很大 | `RUN` 里没清缓存，或数据被 COPY 进去 | `docker history <镜像>` 逐层看体积 |
| 跑完结果不见了 | `-o` 指到了非挂载路径，容器 `--rm` 退出即删 | 输出必须在挂载目录里 |
| `run ... bash` 进不去交互 shell | 少了 `stdin_open` / `tty` | compose 里加 `stdin_open: true` + `tty: true` |
| 容器启动报挂载路径无效 | Windows 路径写法 / 相对路径基准不对 | 原生用绝对路径 `D:\...`；compose 里相对路径是相对 compose 文件 |
| 长任务想中途停 | — | `docker rm -f lorbin-run1` |
| 终端看 apt/pip 中文乱码 | 编码 | `chcp 65001`（cmd） |

### 4.3 本工作区可用的材料

| 材料 | 位置 | 用途 |
|---|---|---|
| 官方源码 | `fromscratch\LorBin\`（干净 clone，commit `ee10232`） | 构建时 COPY 进镜像 |
| 参照结果 | `fromscratch\baseline\golden\`（9 个产物） | S11 对比 |
| 环境快照工具 | `fromscratch\tools\env_probe.sh` | S9 / S12 |
| ⭐ 标准答案 | `fromscratch\docker\Dockerfile`、`fromscratch\docker\docker-compose.yaml`、`fromscratch\.dockerignore` | 自己写完再 diff |
| 数据（已有副本） | `reproduce\LorBin\testdata\` + `reproduce\LorBin\testdata.tar.gz` | S7 的省时替代 |
| 概念材料 | `讲义\`（27 课）、`refs\`（论文精读/术语表） | 想搞懂"为什么要这样设计"时 |

---

## 5. 验收总表（全部打勾才算复现完成）

```
环境（可重建）
[ ] S0  docker info 显示 OSType=linux
[ ] S5  三段自检全部打印正确版本
[ ] S8  镜像 ~1.8 GB；docker history 的 COPY 层只有十几 MB
[ ] S12 删镜像后重建，两份环境快照 fc 无差异

数据（挂载，不进镜像）
[ ] S7  testdata.tar.gz md5 = e6378d903c39d9a383d815ff85797f6d
[ ] S7  fna md5 = 5725ee31ae70727cabf90fb8fa6c58b4
[ ] S7  bam md5 = 76de2a896edea7b3d79145722e8c14e5
[ ] S12 镜像体积里不含这 2.2 GB

运行（跑通且一致）
[ ] S9  env_probe.sh：5 工具 + 7 依赖 + LorBin CLI 全 OK
[ ] S10 容器 Exited (0)；LorBin.log 里能搜到 "to get single-copy markers" 与 "start recluster"
[ ] S10 label.csv 存在；data.csv 是 13,619 行 × 139 列
[ ] S11 label.csv md5 = a12528741f32dda52709f99cdea6af6b
[ ] S11 embedding.csv md5 = 7cbf2784b2b0ac09e794a6583a46146c
[ ] S11 model.pt md5 = 8dbfa00348f38df215be5ec0901db1f3
[ ] S11 output_bins 有 93 个 .fa
[ ] S12 重跑一次，label.csv md5 不变

理解（能讲出来）
[ ] 能解释为什么必须用 Linux 容器（5 个工具的可用性）
[ ] 能解释为什么数据要挂载而不是 COPY（4 条理由）
[ ] 能解释为什么 COPY 源码要放最后一层（层缓存）
[ ] 能解释 data.csv 的 md5 为什么与 baseline 不同却不算错（列名含路径）
[ ] 能解释 `*` 为什么不跨 `/`（2.13 GB 那一课）
```

---

## 6. 迁移到下一个项目：十步检查表

以后拿到任何"只支持 Linux"的科研仓库，照这个顺序做：

```text
□ 1  读 README，回答 8 个问题（S1 的环境信息卡）
□ 2  git ls-files，查 4 样东西：有没有 Dockerfile / conda-requirements / 预打包 / .gitignore
□ 3  判断环境策略（S2 决策树）：有 Docker 就用（但要查它有没有 COPY 数据）；没有就自己写
□ 4  选基础镜像：语言版本对不对 + 包源里有没有要的二进制 + 体积
□ 5  先写 .dockerignore（放上下文根目录；注意 `*` 不跨 `/`）
□ 6  再写 Dockerfile：层顺序"系统 → 语言依赖 → 重依赖 → 自检 → 源码"；偏离版本要写注释
□ 7  最后写 compose：挂载（输入 :ro、输出可写）、工作目录、stdin_open+tty
□ 8  数据放宿主机、用挂载；**绝不 COPY 数据**
□ 9  构建 → 看上下文大小与自检 → 运行期自检并存档 → 跑主命令（产物落挂载目录）
□ 10 对比已知结果 → 回答可复现四问（能重建 / 能重跑一致 / 数据是挂载的 / 别人能跑）
```

**最值得带走的三句话**
1. **"能不能装"决定于操作系统和包源** —— 先查依赖有没有你这个系统的构建，再谈怎么装。
2. **镜像 = 环境，卷 = 数据** —— 混在一起的镜像既大又不可分享（本工作区那个 6.4 GB 就是例子）。
3. **Dockerfile 是环境的源代码** —— 你敲过的命令不是；**能重建的才算复现**。
